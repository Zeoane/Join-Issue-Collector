{
  "name": "join-issue-collector-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Join-Issue Collector API – internal n8n task ingestion",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "stop": "node scripts/stop-dev.js",
    "test": "vitest run"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "firebase-admin": "^13.0.2"
  },
  "devDependencies": {
    "supertest": "^7.0.0",
    "vitest": "^4.1.9"
  }
}
